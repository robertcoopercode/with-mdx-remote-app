# getServerSideProps Problem

Attempting to read from the file system in `getServerSideProps` in `pages/rss.js` works locally, but not when deployed on Vercel.

Deployed URL: https://with-mdx-remote-app.vercel.app/